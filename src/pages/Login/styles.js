import styled from "styled-components";
import LoginBg from "./icons/login_bg.svg";

export const Container = styled.div`
  height: calc(100vh - 0px);
  width: 100%;
  background-size: cover;
  background-image: url(${LoginBg});
`;
